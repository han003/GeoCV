@model GeoCV.Models.LeggTilTekniskProfilModel

<div id="legg-til-teknisk-profil-modal" class="modal fade">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Legg til</h4>
            </div>
            <div class="modal-body">

                @using (Html.BeginForm("LeggTilProfil", "TekniskeProfiler", FormMethod.Post, new { @class = "form-horizontal", id = "legg-til-form" }))
                {
                    @Html.AntiForgeryToken()

    <div class="form-group">
        @Html.HiddenFor(m => m.ProsjektId, new { id = "legg-til-prosjekt-id" })
    </div>

                    <div class="form-group">
                        @Html.LabelFor(m => m.ProfilNavn, new { @class = "col-md-2 control-label" })
                        <div class="col-md-10">
                            @Html.TextBoxFor(m => m.ProfilNavn, new { @class = "form-control", autocomplete = "off" })
                        </div>
                        <div class="col-md-10 col-md-offset-2">
                            @Html.ValidationMessageFor(m => m.ProfilNavn, null, new { @class = "text-danger" })
                        </div>
                    </div>
                }

            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal">Avbryt</button>
                <input type="submit" class="btn btn-primary" form="legg-til-form" value="Legg til" />
            </div>
        </div>
    </div>
</div>
