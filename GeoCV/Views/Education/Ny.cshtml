@using System.Web.Optimization
@using Microsoft.AspNet.Identity

@model GeoCV.Models.CVVersjon

@{
    ViewBag.Title = "Legg til utdannelse";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section css{
    @Styles.Render("~/Bundles/css/education")
}
@section scripts{
    @Scripts.Render("~/Bundles/js/education")
}

<div id="page-wrapper">
    <div class="row">
        <div class="col-sm-12">
            <h1 class="page-header">Legg til utdannelse</h1>
        </div>
    </div>

    <div class="row">
        <div class="col-sm-12">

            <div class="form-horizontal">
                <div class="form-group">
                    <label for="school-text" class="col-lg-2 control-label">Studiested</label>
                    <div class="col-lg-8">
                        <input id="school-text" type="text" class="form-control" autocomplete="off">
                    </div>
                    <div id="school-label" class="col-lg-2 side-label hidden">
                        <span class="label label-warning">Oppdaterer <i class="fa fa-spinner fa-pulse"></i></span>
                    </div>
                </div>

                <div class="form-group">
                    <label for="education-text" class="col-lg-2 control-label">Beskrivelse</label>
                    <div class="col-lg-8">
                        <input id="education-text" type="text" class="form-control" autocomplete="off">
                    </div>
                    <div id="education-label" class="col-lg-2 side-label hidden">
                        <span class="label label-warning">Oppdaterer <i class="fa fa-spinner fa-pulse"></i></span>
                    </div>
                </div>

                <div class="form-group">
                    <label for="education-select-from" class="col-lg-2 control-label">Fra</label>
                    <div class="col-lg-8">
                        <select id="education-select-from" class="form-control">
                            @for (var i = 0; i <= 100; i++)
                            {
                                <option>@(@DateTime.Now.Year - i)</option>
                            }
                        </select>
                    </div>
                    <div id="education-select-from-label" class="col-lg-2 side-label hidden">
                        <span class="label label-warning">Oppdaterer <i class="fa fa-spinner fa-pulse"></i></span>
                    </div>
                </div>

                <div class="form-group">
                    <label for="education-select-to" class="col-lg-2 control-label">Til</label>
                    <div class="col-lg-8">
                        <select id="education-select-to" class="form-control">
                            @for (var i = 0; i <= 100; i++)
                            {
                                <option>@(@DateTime.Now.Year - i)</option>
                            }
                        </select>
                    </div>
                    <div id="education-select-to-label" class="col-lg-2 side-label hidden">
                        <span class="label label-warning">Oppdaterer <i class="fa fa-spinner fa-pulse"></i></span>
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-lg-8 col-lg-offset-2">
                        <button id="education-add-btn" class="btn btn-success" type="button">Legg til utdannelse</button>
                    </div>
                    <div id="education-label" class="col-lg-2 side-label hidden">
                        <span class="label label-warning">Oppdaterer <i class="fa fa-spinner fa-pulse"></i></span>
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>
